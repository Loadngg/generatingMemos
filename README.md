# An application for generating TSTU service notes using excel spreadsheets and templates

## Requirements

Python3.8 or later

## Getting started

```shell
git clone https://github.com/Loadngg/generatingMemos.git

python3 -m venv .venv

. ./.venv/bin/activate

pip3 install -r requirements.txt

python3 main.py
```
